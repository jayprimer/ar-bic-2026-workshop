# Stage 2 input config — edit this file to change what the screener does.
#
# Format: KEY = value, '#' comments, indent to continue a value across lines.
# (Same parser as stage_01/input.txt.)

# OpenAI model used when OPENAI_API_KEY is set. With no key, Stage 2
# falls back to the keyword/regex emulator and this value is ignored.
MODEL = gpt-5.4-nano

# Inclusion/exclusion criteria. The screener inlines this file into
# the LLM prompt and uses its keywords to tune the regex emulator.
# Path is relative to the cwd from which 02_screen.py is invoked.
CRITERIA_FILE = stage_02/criteria.txt

# Pause between OpenAI API calls (seconds). 1.0 is conservative for
# free-tier accounts; lower if you have higher rate limits.
SLEEP_SECONDS = 1.0
